import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('placeholder', (tester) async {
    // Testes serão adicionados conforme as features forem desenvolvidas.
    expect(true, isTrue);
  });
}
